{
  "name": "@duke-dsh-plugins/dsh-agent-approval",
  "version": "1.6.0",
  "description": "Agent-decided approvals for DeepSeek Harness: a workspace-write base permission mode where an independent approval subagent judges every sandbox escalation (risky operations are rejected), with a configurable approval model and a per-session audit trail in the conversation window's 审批 tab.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MoonlitDropOfBlood/dsh-agent-approval.git"
  },
  "homepage": "https://github.com/MoonlitDropOfBlood/dsh-agent-approval",
  "bugs": { "url": "https://github.com/MoonlitDropOfBlood/dsh-agent-approval/issues" },
  "publishConfig": { "access": "public" },
  "keywords": ["dsh", "deepseek-harness", "plugin", "approval", "security"],
  "type": "module",
  "main": "index.js",
  "exports": {
    ".": "./index.js",
    "./client": "./client.js",
    "./typert": "./typert.host.js",
    "./cordis.patch.yml": "./cordis.patch.yml",
    "./package.json": "./package.json"
  },
  "files": [
    "index.js",
    "lib",
    "client.js",
    "typert.host.js",
    "cordis.patch.yml",
    "test",
    "scripts"
  ],
  "license": "MIT",
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web"
    }
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/dsh-typert-protocol": "^0.1.0-rc.7"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "scripts": {
    "patch:glyph": "node scripts/patch-glyph.mjs",
    "test": "node --test test/",
    "check": "node --check index.js && node --check lib/pure.js && node --check client.js && node --check typert.host.js && node --check scripts/patch-glyph.mjs && node --check scripts/check-session-log.mjs && node --check scripts/repair-session-log.mjs && node --check scripts/migrate-records.mjs",
    "verify": "npm run check && npm test"
  }
}
